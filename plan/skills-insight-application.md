# Claude Code Skills 인사이트 적용 계획

## 개요

Claude Code Skills 생태계(Superpowers, Humanizer, UI/UX Pro Max, Find Skills) 분석을 통해
현재 trading-sandbox 프로젝트에 적용 가능한 4가지 개선 방향을 도출하고 실행 계획을 수립합니다.

> 참고 문서: [docs/learning/claude-code-skills-guide.md](../docs/learning/claude-code-skills-guide.md)

---

## 현재 프로젝트 구조 진단

```
trading-sandbox/
├── scripts/
│   ├── main.py                  # CLI 진입점 (227줄)
│   ├── briefing_generator.py    # 핵심 파이프라인 (463줄) ← 개선 주 대상
│   └── collectors/
│       ├── dart_collector.py    # 공시 수집 (212줄)
│       ├── krx_collector.py     # 주가 수집 (327줄)
│       ├── ecos_collector.py    # 경제지표 수집 (409줄)
│       └── news_collector.py    # 뉴스 수집 (205줄)
├── config/settings.py           # 중앙 설정
├── plan/                        # 본 파일 위치
├── hooks/                       # Claude 트리거
└── .github/workflows/           # GitHub Actions
```

### 현재 데이터 흐름

```
[main.py --type morning/midday/aftermarket]
    ↓
[briefing_generator.py collect_all_data()]
    ├── dart_collector.format_for_briefing()    ← 순차 실행
    ├── krx_collector.format_for_briefing()     ← 순차 실행
    ├── ecos_collector.format_for_briefing()    ← 순차 실행
    └── news_collector.format_for_briefing()    ← 순차 실행
    ↓
[generate_with_ai() or generate_basic_briefing()]
    ↓
[notes/daily_briefing/YYYY-MM-DD_XX브리핑.md 저장]
```

---

## 적용 방안 1: Superpowers 방식 — 설계 우선 개발 원칙

### Skills 인사이트
Superpowers는 AI 에이전트가 **코드 작성 전에 반드시 설계 문서를 작성**하게 만든다.
큰 작업에서 방향을 잃지 않고 오랜 시간 자율적으로 작업 가능한 이유가 여기에 있다.

### 현재 문제점
- 새 기능 요청 시 Claude Code가 즉시 코드를 수정하면서 방향을 놓치는 사례 발생
- 예: 3분기 브리핑 분리, 거시경제 지표 확장 — 사전 설계 없이 진행해 여러 번 수정

### 적용 목표
Claude Code가 이 프로젝트에서 작업할 때 **자동으로 plan/ 폴더를 먼저 확인하고 설계한 뒤 구현**하는 습관을 만든다.

### 실행 항목

**A. CLAUDE.md 파일 생성** (프로젝트 루트)

```markdown
# 개발 원칙

## 새 기능 개발 순서
1. plan/ 폴더에 설계 문서 작성 (사용자 승인 후 구현)
2. 기존 관련 파일을 모두 읽은 뒤 수정 시작
3. 수정 완료 후 관련 docs/ 문서 업데이트
4. GitHub 커밋

## 금지 사항
- plan/ 없이 100줄 이상의 코드 변경 금지
- config/settings.py 변경 시 반드시 .env.example도 함께 확인
```

**B. 기능 설계 템플릿 생성** `plan/templates/feature-design.md`

```markdown
# [기능명] 설계 문서

## 목적
## 현재 동작 (Before)
## 변경 후 동작 (After)
## 수정 대상 파일
## 예상 부작용
## 롤백 방법
```

- [x] CLAUDE.md 파일 프로젝트 루트에 생성 ✅ 2026-02-11
- [ ] plan/templates/ 디렉토리 및 feature-design.md 템플릿 생성
- [ ] plan/README.md에 설계 원칙 섹션 추가

---

## 적용 방안 2: Humanizer 방식 — AI 브리핑 텍스트 품질 개선

### Skills 인사이트
AI가 생성한 텍스트가 평범한 이유는 **통계적으로 가장 빈번한 표현**을 사용하기 때문이다.
24가지 패턴을 명시적 금지 목록으로 정의하면 결과물이 달라진다.

### 현재 문제점
실제 브리핑 파일에서 발견된 AI 냄새 표현 패턴:

| 발견 위치 | 문제 표현 | 원인 |
|---------|---------|------|
| 2026-02-10 애프터마켓 | "안정적인 거래량을 기록했습니다" | 거래량 수치만 있고 판단 근거 없음 |
| 2026-02-10 애프터마켓 | "긍정적인 영향을 미칠 것으로 예상됩니다" | 구체적 근거 없는 예측 |
| 2026-02-11 애프터마켓 | "큰 변동성을 보이며 상승세를 이어갔습니다" | 이유 없는 단순 서술 |
| 여러 파일 | 모든 종목 동일 톤으로 서술 | 종목별 차별화 없음 |

> 직접 경험한 오분석 사례: 엑스페릭스 -10%~+15% 변동을 "변동성 크지 않았다"로 서술

### 적용 목표
`scripts/briefing_generator.py`의 AI 시스템 프롬프트에 **한국어 금지 표현 목록** 추가

### 실행 항목

**A. 시스템 프롬프트에 금지 표현 섹션 추가**

```python
# briefing_generator.py AI_SYSTEM_PROMPT에 추가할 내용

"""
## 절대 사용 금지 표현
- "~할 것으로 예상됩니다" → 대신: 구체적 수치 + "를 기록했다"
- "안정적인 상승세/하락세" → 대신: 실제 등락률(%) 수치 명시
- "긍정적/부정적인 영향" → 대신: 어떤 지표가 몇 % 변화했는지 서술
- "주목할 필요가 있습니다" → 근거 없으면 삭제
- "큰 변동성을 보였습니다" → 고가-저가 스프레드 % 수치로 대체

## 필수 작성 규칙
- 모든 평가 문장에는 반드시 수치 근거 포함
- 각 종목은 서로 다른 관점으로 서술 (동일 톤 반복 금지)
- 등락률 ±3% 이상 종목은 반드시 원인 분석 포함
"""
```

**B. 브리핑 품질 검증 체크리스트 추가** (선택적 구현)

```python
# briefing_generator.py에 post-processing 함수 추가 검토
BANNED_PHRASES_KO = [
    "할 것으로 예상됩니다",
    "안정적인 상승세",
    "긍정적인 영향",
    "주목할 필요가 있습니다",
]
```

- [x] `AI_SYSTEM_PROMPT` 금지 표현 목록 섹션 추가 ✅ 2026-02-11
- [ ] 모닝/미드데이/애프터마켓 각 프롬프트에 "수치 없는 평가 금지" 규칙 추가
- [ ] 기존 브리핑 파일 검토해 금지 목록 추가 보강

---

## 적용 방안 3: 병렬 처리 — 데이터 수집 성능 개선

### Skills 인사이트
Peter의 작업 환경처럼 에이전트를 **병렬로 실행**하면 같은 시간에 더 많은 작업이 가능하다.
현재 순차적으로 실행되는 데이터 수집 과정을 병렬화하면 수집 시간을 크게 단축할 수 있다.

### 현재 문제점
`briefing_generator.py`의 `collect_all_data()` 메서드:

```python
# 현재 구조 (순차 실행)
def collect_all_data(self, briefing_type):
    krx_text = self.krx.format_for_briefing()       # ~3-5초 (KRX 서버)
    ecos_text = self.ecos.format_for_briefing()     # ~3-8초 (ECOS + FRED)
    dart_text = self.dart.format_for_briefing(...)  # ~2-4초 (DART)
    news_text = self.news.format_for_briefing(...)  # ~3-6초 (RSS 3개)
    # 총 ~11-23초 (순차)
```

### 적용 목표
4개 collector를 동시 실행해 수집 시간을 **~5-8초로 단축** (예상 50~60% 절감)

### 실행 항목

**A. `collect_all_data()` 병렬화**

```python
# 변경 후 (병렬 실행)
from concurrent.futures import ThreadPoolExecutor, as_completed

def collect_all_data(self, briefing_type):
    settings = BRIEFING_SETTINGS[briefing_type]

    def fetch_krx():
        return self.krx.format_for_briefing()

    def fetch_ecos():
        return self.ecos.format_for_briefing()

    def fetch_dart():
        return self.dart.format_for_briefing(
            days_back=settings["days_back"],
            max_items=settings["max_disclosures"]
        )

    def fetch_news():
        return self.news.format_for_briefing(
            max_items=settings["max_news"],
            max_hours=settings["news_max_hours"]
        )

    with ThreadPoolExecutor(max_workers=4) as executor:
        futures = {
            "krx": executor.submit(fetch_krx),
            "ecos": executor.submit(fetch_ecos),
            "dart": executor.submit(fetch_dart),
            "news": executor.submit(fetch_news),
        }
        results = {key: future.result() for key, future in futures.items()}

    return results  # {"krx": str, "ecos": str, "dart": str, "news": str}
```

**B. 수집 시간 로깅 추가**

```python
import time
start = time.time()
# ... 병렬 수집 ...
elapsed = time.time() - start
print(f"[수집 완료] {elapsed:.1f}초 소요")
```

- [ ] `briefing_generator.py`의 `collect_all_data()` 병렬화 구현
- [ ] 수집 시간 측정 로깅 추가
- [ ] 병렬화 전후 시간 비교 테스트

---

## 적용 방안 4: Structured Outputs — AI 분석 결과 구조화

### Skills 인사이트
Superpowers의 "설계 → 구현" 원칙처럼, **AI 응답 스키마를 먼저 정의**하고 코드를 짜야 한다.
현재 줄글로만 반환되는 AI 분석을 구조화하면 DB 저장, 알림 트리거 등 재사용이 가능해진다.

> 상세 내용: [docs/learning/structured-outputs-guide.md](../docs/learning/structured-outputs-guide.md)

### 현재 문제점
- AI 분석이 줄글(markdown text)로만 반환 → 종목별 평가 점수 추출 불가
- `daily-briefing-expansion.md`의 "알림 시스템" 기능 구현 불가 (구조화 데이터 없음)

### 적용 목표
AI 분석 결과를 `.md`(사람용) + `.json`(기계용)으로 동시 저장

```
notes/daily_briefing/
├── 2026-02-11_애프터마켓브리핑.md    ← 기존 (마크다운)
└── 2026-02-11_애프터마켓브리핑.json  ← 신규 (구조화 데이터)
```

### Pydantic 모델 설계 (초안)

```python
from pydantic import BaseModel

class StockInsight(BaseModel):
    ticker: str           # "005930"
    name: str             # "삼성전자"
    close: int            # 167800
    change_pct: float     # +1.21
    assessment: str       # "상승" | "하락" | "보합"
    key_reason: str       # 등락 원인 한 줄 요약
    watch_point: str      # 내일 주목 포인트

class MarketAnalysis(BaseModel):
    date: str             # "2026-02-11"
    briefing_type: str    # "aftermarket"
    kospi_trend: str      # 시장 전반 한 줄 요약
    key_macro_event: str  # 오늘 주요 거시 이벤트
    stocks: list[StockInsight]
    tomorrow_strategy: str  # 내일 전략 요약
```

### 실행 항목
- [ ] `MarketAnalysis`, `StockInsight` Pydantic 모델 설계 확정
- [ ] `briefing_generator.py`에 `generate_with_ai_structured()` 메서드 추가
- [ ] `.md` + `.json` 이중 저장 구조 구현
- [ ] JSON 결과를 이용한 임계값 알림 기능 (등락률 ±5% → 콘솔 경고)

---

## 우선순위 및 실행 순서

| 우선순위 | 방안 | 대상 파일 | 예상 효과 | 난이도 |
|---------|------|----------|----------|--------|
| **1** | Humanizer — AI 프롬프트 금지 표현 추가 | `briefing_generator.py` | 즉각적 품질 개선 | 낮음 |
| **2** | Superpowers — CLAUDE.md 및 설계 원칙 수립 | `CLAUDE.md` (신규) | 장기 개발 일관성 | 낮음 |
| **3** | 병렬 처리 — 수집 성능 개선 | `briefing_generator.py` | 수집 시간 50%↓ | 중간 |
| **4** | Structured Outputs — AI 결과 구조화 | `briefing_generator.py` | 알림/DB 확장 가능 | 높음 |

---

## 연계 계획

이 계획은 기존 계획들과 다음과 같이 연결됩니다:

| 연계 계획 파일 | 관련 항목 |
|-------------|---------|
| [daily-briefing-expansion.md](daily-briefing-expansion.md) | 알림 시스템 → Structured Outputs 선행 필요 |
| [pain-points-improvement.md](pain-points-improvement.md) | AI 분석 품질 → Humanizer 방식으로 개선 |

---

*작성일: 2026년 2월 11일*
*마지막 업데이트: 2026년 2월 11일*
*상태: 🔄 진행 중 (Phase 1 완료)*
